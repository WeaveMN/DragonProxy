machine:
  java:
    version: oraclejdk8

checkout:
    post:
        - git submodule sync
        - git submodule update --init --recursive
        - (cd MCProtocolLib && mvn install)
        - mvn package
test:
    post:
        - cp target/*.jar $CIRCLE_ARTIFACTS/
